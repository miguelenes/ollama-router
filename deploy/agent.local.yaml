# Loopback serve config for `task dev` / `task agent:serve`.
# Packaged agents default to loopback + a zrok sidecar; local-dev does not.
# Do not run `setup` — Ollama is already a host service.
listen: loopback
port: 11436
ollama:
  listen: loopback
gpu:
  policy: auto
tunnel:
  enable: false
