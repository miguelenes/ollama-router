# Copy to runner.env and fill in. NEVER commit this file.
# Used by: docker compose --profile runner --env-file runner.env up -d
REPO_URL=https://github.com/miguelenes/ollama-router
ACCESS_TOKEN=
RUNNER_NAME=nas-ollama-router
RUNNER_LABELS=self-hosted,linux,ollama-router
