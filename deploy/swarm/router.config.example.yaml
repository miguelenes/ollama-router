# Placeholder tunables overlay for the Swarm stack (`OLLAMA_ROUTER_CONFIG`).
# Commit only placeholders — never live values, tokens, or fleet inventory.
#
# Copy this to `/etc/ollama-router/config.yaml` on the swarm manager (or
# mount a real overlay here) and keep `nodes:` OUT of it: membership is
# fleet.yaml + FleetState + cloud managers; a top-level `nodes:` key in a
# tunables overlay is a hard config error.
#
# Example knobs (defaults live in router.defaults.yaml at the repo root):
# policy:
#   default_max_inflight: 2
# saturation_wait_seconds: 0
# auto_pull_on_miss: false
job_store_path: /var/lib/ollama-router/model-operations.sqlite3
