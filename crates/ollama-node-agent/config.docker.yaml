# Loopback defaults for the root Dockerfile `agent` target
# (`docker build --target agent`). Fleet hosts use systemd +
# /etc/ollama-node-agent/config.yaml (typically listen: loopback + tunnel).
listen: loopback
port: 11436
ollama:
  listen: loopback
gpu:
  policy: auto
tunnel:
  enable: false
