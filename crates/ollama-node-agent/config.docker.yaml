# Loopback defaults for Dockerfile.agent. Fleet hosts use systemd +
# /etc/ollama-node-agent/config.yaml (typically listen: tailscale).
listen: loopback
port: 11436
ollama:
  listen: loopback
gpu:
  policy: auto
tailscale:
  enable: false
